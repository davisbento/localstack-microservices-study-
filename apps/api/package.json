{
  "name": "@localstack-microservices/api",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "targets": {
      "serve": {
        "continuous": true,
        "executor": "@nx/js:node",
        "defaultConfiguration": "development",
        "dependsOn": [
          "build"
        ],
        "options": {
          "buildTarget": "@localstack-microservices/api:build",
          "runBuildTargetDependencies": false
        },
        "configurations": {
          "development": {
            "buildTarget": "@localstack-microservices/api:build:development"
          },
          "production": {
            "buildTarget": "@localstack-microservices/api:build:production"
          }
        }
      }
    }
  }
}
